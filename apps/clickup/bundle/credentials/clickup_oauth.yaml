name: clickup_oauth
type: OAUTH2_CREDENTIALS
oauth2:
  clientId: clickup_client_id
  clientSecret: clickup_client_secret
  tokenUrl: clickup_token_url
  authorizeUrl: clickup_authorize_url
  # authorizationHeader: ${token}
